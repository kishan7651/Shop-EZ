## Packages
framer-motion | Page transitions and scroll-triggered animations

## Notes
The application assumes a standard REST API backend.
Authentication uses HTTP-only cookies, handled by browser automatically.
Prices from the backend (PostgreSQL decimal) might be strings, require parsing to numbers for client-side calculations.
